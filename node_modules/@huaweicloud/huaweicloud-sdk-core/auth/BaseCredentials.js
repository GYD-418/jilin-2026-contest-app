"use strict";
/*
 * Copyright 2020 Huawei Technologies Co.,Ltd.
 *
 * Licensed to the Apache Software Foundation (ASF) under one
 * or more contributor license agreements.  See the NOTICE file
 * distributed with this work for additional information
 * regarding copyright ownership.  The ASF licenses this file
 * to you under the Apache License, Version 2.0 (the
 * "License"); you may not use this file except in compliance
 * with the License.  You may obtain a copy of the License at
 *
 *     http://www.apache.org/licenses/LICENSE-2.0
 *
 * Unless required by applicable law or agreed to in writing,
 * software distributed under the License is distributed on an
 * "AS IS" BASIS, WITHOUT WARRANTIES OR CONDITIONS OF ANY
 * KIND, either express or implied.  See the License for the
 * specific language governing permissions and limitations
 * under the License.
 */
var __createBinding = (this && this.__createBinding) || (Object.create ? (function(o, m, k, k2) {
    if (k2 === undefined) k2 = k;
    var desc = Object.getOwnPropertyDescriptor(m, k);
    if (!desc || ("get" in desc ? !m.__esModule : desc.writable || desc.configurable)) {
      desc = { enumerable: true, get: function() { return m[k]; } };
    }
    Object.defineProperty(o, k2, desc);
}) : (function(o, m, k, k2) {
    if (k2 === undefined) k2 = k;
    o[k2] = m[k];
}));
var __setModuleDefault = (this && this.__setModuleDefault) || (Object.create ? (function(o, v) {
    Object.defineProperty(o, "default", { enumerable: true, value: v });
}) : function(o, v) {
    o["default"] = v;
});
var __importStar = (this && this.__importStar) || function (mod) {
    if (mod && mod.__esModule) return mod;
    var result = {};
    if (mod != null) for (var k in mod) if (k !== "default" && Object.prototype.hasOwnProperty.call(mod, k)) __createBinding(result, mod, k);
    __setModuleDefault(result, mod);
    return result;
};
Object.defineProperty(exports, "__esModule", { value: true });
exports.BaseCredentials = void 0;
const ICredential_1 = require("./ICredential");
const AKSKSigner_1 = require("./AKSKSigner");
const IHttpRequestBuilder_1 = require("../http/IHttpRequestBuilder");
const iam_service_1 = require("../internal/services/iam.service");
const authcache_1 = require("../internal/services/authcache");
const DerivedAKSKSigner_1 = require("./DerivedAKSKSigner");
const fs = __importStar(require("fs"));
const SdkException_1 = require("../exception/SdkException");
class BaseCredentials {
    constructor(data) {
        this.ak = data === null || data === void 0 ? void 0 : data.ak;
        this.sk = data === null || data === void 0 ? void 0 : data.sk;
        this.securityToken = data === null || data === void 0 ? void 0 : data.securityToken;
        this.iamEndpoint = data === null || data === void 0 ? void 0 : data.iamEndpoint;
        this.idpId = data === null || data === void 0 ? void 0 : data.idpId;
        this.idTokenFile = data === null || data === void 0 ? void 0 : data.idTokenFile;
        this.expiredAt = data === null || data === void 0 ? void 0 : data.expiredAt;
        this.regionId = data === null || data === void 0 ? void 0 : data.regionId;
        this.derivedAuthServiceName = data === null || data === void 0 ? void 0 : data.derivedAuthServiceName;
        this.derivedPredicate = data === null || data === void 0 ? void 0 : data.derivedPredicate;
    }
    withAk(ak) {
        this.ak = ak;
        return this;
    }
    withSk(sk) {
        this.sk = sk;
        return this;
    }
    withSecurityToken(securityToken) {
        this.securityToken = securityToken;
        return this;
    }
    withIamEndpoint(iamEndpoint) {
        this.iamEndpoint = iamEndpoint;
        return this;
    }
    withIdpId(idpId) {
        this.idpId = idpId;
        return this;
    }
    withIdTokenFile(idTokenFile) {
        this.idTokenFile = idTokenFile;
        return this;
    }
    getDerivedAuthServiceName() {
        return this.derivedAuthServiceName;
    }
    processDerivedAuthParams(derivedAuthServiceName, regionId) {
        if (derivedAuthServiceName) {
            this.derivedAuthServiceName = derivedAuthServiceName;
        }
        if (regionId) {
            this.regionId = regionId;
        }
    }
    static getDefaultDerivedPredicate(request) {
        var _a;
        const hostname = (_a = new URL(request === null || request === void 0 ? void 0 : request.endpoint)) === null || _a === void 0 ? void 0 : _a.hostname;
        return !this.DEFAULT_ENDPOINT_REG.test(hostname);
    }
    withRegionId(regionId) {
        this.regionId = regionId;
        return this;
    }
    getAk() {
        return this.ak;
    }
    getSk() {
        return this.sk;
    }
    getSecurityToken() {
        return this.securityToken;
    }
    getIamEndpoint() {
        return this.iamEndpoint;
    }
    getIdpId() {
        return this.idpId;
    }
    getIdTokenFile() {
        return this.idTokenFile;
    }
    getExpiredAt() {
        return this.expiredAt;
    }
    getRegionId() {
        return this.regionId;
    }
    async processAuthRequest(hcClient, httpRequest) {
        if (this.needUpdateFederalAuthToken()) {
            await this.updateFederalAuthTokenByIdToken(hcClient);
        }
        await this.validateCredentials();
        const builder = new IHttpRequestBuilder_1.HttpRequestBuilder();
        builder.addPathParams(this.getPathParams());
        let url = httpRequest.url;
        const pathParams = this.getPathParams();
        if (Object.keys(pathParams).length > 0) {
            url = this.parsePath(url, pathParams);
            builder.withUrl(url);
        }
        builder.withEndpoint(`${httpRequest.endpoint}${url}`);
        this.addAuthHeaders(builder);
        if (!(0, ICredential_1.isJsonContentType)(httpRequest.headers)) {
            builder.addHeaders('X-Sdk-Content-Sha256', 'UNSIGNED-PAYLOAD');
        }
        builder.addAllHeaders(httpRequest.headers);
        Object.assign(httpRequest, builder.build());
        const headers = this.isDerivedAuth(httpRequest)
            ? DerivedAKSKSigner_1.DerivedAKSKSigner.sign(httpRequest, this)
            : AKSKSigner_1.AKSKSigner.sign(httpRequest, this);
        builder.addAllHeaders(headers);
        return Object.assign(httpRequest, builder.build());
    }
    async validateCredentials() {
        if (!this.ak) {
            throw new AKSKSigner_1.RequiredError('AK cannot be empty or undefined.');
        }
        if (!this.sk) {
            throw new AKSKSigner_1.RequiredError('SK cannot be empty or undefined.');
        }
    }
    addAuthHeaders(builder) {
        if (this.securityToken) {
            builder.addHeaders('X-Security-Token', this.securityToken);
        }
        const authParam = this.getPathParams()[this.getAuthParamName()];
        if (authParam) {
            builder.addHeaders(this.getAuthHeaderName(), authParam);
        }
    }
    async processAuthParams(hcClient, region) {
        var _a, _b;
        const authParam = this.getPathParams()[this.getAuthParamName()];
        if (authParam) {
            return this;
        }
        const authCacheInstance = authcache_1.AuthCache.instance();
        let cacheName = '';
        if (this.getAk() !== null && ((_a = this.getAk()) === null || _a === void 0 ? void 0 : _a.trim()) !== '') {
            cacheName = this.getAk() + (region !== null && region !== void 0 ? region : '') + this.getAuthParamName();
        }
        else if (this.getIdpId() !== null && ((_b = this.getIdpId()) === null || _b === void 0 ? void 0 : _b.trim()) !== '') {
            cacheName = this.getIdpId() + (region !== null && region !== void 0 ? region : '') + this.getAuthParamName();
        }
        const cachedParam = authCacheInstance.getCache(cacheName);
        if (cachedParam) {
            this.setAuthParam(cachedParam);
            return this;
        }
        const param = await this.getAuthParamFromIam(hcClient, region);
        authCacheInstance.putCache(cacheName, param);
        this.setAuthParam(param);
        return this;
    }
    parsePath(path, params) {
        if (!path || !params) {
            return path;
        }
        return Object.keys(params).reduce((parsedPath, param) => {
            const value = encodeURIComponent(params[param]);
            return parsedPath.replace(new RegExp(`{${param}}`), value);
        }, path);
    }
    needUpdateFederalAuthToken() {
        if (!this.idpId || !this.idTokenFile) {
            return false;
        }
        if (!this.securityToken || !this.expiredAt) {
            return true;
        }
        return (this.expiredAt - Date.now() <
            BaseCredentials.DEFAULT_EXPIRATION_THRESHOLD_SECONDS * 1000);
    }
    async updateFederalAuthTokenByIdToken(hcClient) {
        if (!this.idTokenFile || !this.idpId) {
            throw new AKSKSigner_1.RequiredError('idTokenFile or idpId is null.');
        }
        const idToken = fs.readFileSync(this.idTokenFile, 'utf-8').trim();
        if (!idToken) {
            throw new AKSKSigner_1.RequiredError('idTokenFile content is null, idTokenFile: ', this.idTokenFile);
        }
        const iamService = new iam_service_1.IamService(hcClient, this.iamEndpoint, true);
        try {
            const unscopedToken = await iamService.getUnscopedTokenWithIdToken(idToken, this.idpId);
            const response = await iamService.getTemporaryAccessKeyByToken(unscopedToken);
            this.ak = response.access;
            this.sk = response.secret;
            this.securityToken = response.securitytoken;
            const resExpiresAt = response.expires_at;
            if (resExpiresAt) {
                const expiredTime = resExpiresAt.replace('000Z', 'Z');
                this.expiredAt = new Date(expiredTime).getTime();
            }
        }
        catch (error) {
            throw new SdkException_1.SdkException(`Failed to update federal auth token by ID token: ${error}`);
        }
    }
    withDerivedPredicate(derivedPredicate) {
        this.derivedPredicate = derivedPredicate;
        return this;
    }
    isDerivedAuth(httpRequest) {
        if (!this.derivedPredicate) {
            return false;
        }
        return this.derivedPredicate(httpRequest);
    }
}
exports.BaseCredentials = BaseCredentials;
BaseCredentials.DEFAULT_ENDPOINT_REG = /^(?<firstDomainNameLetter>[a-z])(?<firstDomainName>[a-z0-9-]+)(?<middleDomainName>(?<middleDomainPattern>\.[a-z]{2,}-[a-z]+-\d{1,2})?)(?<lastDomainName>\.(?<myPrefix>my)?(?<cloudProvider>huaweicloud|myhwclouds)).(?<domainSuffix>(?<topLevelDomain>com|cn))$/;
BaseCredentials.DEFAULT_EXPIRATION_THRESHOLD_SECONDS = 40 * 60; // 2h
//# sourceMappingURL=BaseCredentials.js.map