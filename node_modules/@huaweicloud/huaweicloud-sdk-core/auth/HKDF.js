"use strict";
/*
 * Copyright 2020 Huawei Technologies Co.,Ltd.
 *
 * Licensed to the Apache Software Foundation (ASF) under one
 * or more contributor license agreements.  See the NOTICE file
 * distributed with this work for additional information
 * regarding copyright ownership.  The ASF licenses this file
 * to you under the Apache License, Version 2.0 (the
 * "License"); you may not use this file except in compliance
 * with the License.  You may obtain a copy of the License at
 *
 *     http://www.apache.org/licenses/LICENSE-2.0
 *
 * Unless required by applicable law or agreed to in writing,
 * software distributed under the License is distributed on an
 * "AS IS" BASIS, WITHOUT WARRANTIES OR CONDITIONS OF ANY
 * KIND, either express or implied.  See the License for the
 * specific language governing permissions and limitations
 * under the License.
 */
var __createBinding = (this && this.__createBinding) || (Object.create ? (function(o, m, k, k2) {
    if (k2 === undefined) k2 = k;
    var desc = Object.getOwnPropertyDescriptor(m, k);
    if (!desc || ("get" in desc ? !m.__esModule : desc.writable || desc.configurable)) {
      desc = { enumerable: true, get: function() { return m[k]; } };
    }
    Object.defineProperty(o, k2, desc);
}) : (function(o, m, k, k2) {
    if (k2 === undefined) k2 = k;
    o[k2] = m[k];
}));
var __setModuleDefault = (this && this.__setModuleDefault) || (Object.create ? (function(o, v) {
    Object.defineProperty(o, "default", { enumerable: true, value: v });
}) : function(o, v) {
    o["default"] = v;
});
var __importStar = (this && this.__importStar) || function (mod) {
    if (mod && mod.__esModule) return mod;
    var result = {};
    if (mod != null) for (var k in mod) if (k !== "default" && Object.prototype.hasOwnProperty.call(mod, k)) __createBinding(result, mod, k);
    __setModuleDefault(result, mod);
    return result;
};
Object.defineProperty(exports, "__esModule", { value: true });
exports.HKDF = void 0;
const crypto = __importStar(require("crypto"));
const SdkException_1 = require("../exception/SdkException");
/**
 * HKDF是一种基于HMAC的密钥导出函数，分为提取和扩展两步，用于从短密钥材料生成更长的密钥
 */
class HKDF {
    static getDerKeySHA256(accessKey, secretKey, info) {
        if (!accessKey || !secretKey) {
            return null;
        }
        try {
            const tmpKey = HKDF.extract(HKDF.stringToBinary(secretKey), HKDF.stringToBinary(accessKey), HKDF.HMAC_ALGORITHM);
            const derSecretKey = HKDF.expand(tmpKey, HKDF.stringToBinary(info), HKDF.HMAC_ALGORITHM, HKDF.DERIVATION_KEY_LENGTH, HKDF.EXPAND_CEIL);
            if (!derSecretKey) {
                return null;
            }
            return HKDF.toHex(derSecretKey);
        }
        catch (e) {
            throw new SdkException_1.SdkException(`Failed to derive AK ${accessKey} with info ${info}`);
        }
    }
    static toHex(data) {
        return data.toString('hex').toLowerCase();
    }
    static extract(ikm, salt, hmacAlgorithm) {
        let saltTemp = salt;
        if (salt === null || salt.length === 0) {
            saltTemp = Buffer.alloc(HKDF.getHashLen(hmacAlgorithm));
        }
        return HKDF.hmacSHA256(ikm, saltTemp, hmacAlgorithm);
    }
    static expand(prk, info, hmacAlgorithm, okmLength, ceil) {
        let rawResult = Buffer.alloc(0);
        if (ceil === 1) {
            rawResult = HKDF.expandFirst(prk, info, hmacAlgorithm);
        }
        else {
            let temp = Buffer.alloc(0);
            for (let i = 1; i <= ceil; ++i) {
                temp = HKDF.expandOnce(prk, info, temp, i, hmacAlgorithm);
                rawResult = Buffer.concat([rawResult, temp], rawResult.length + temp.length);
            }
        }
        if (okmLength === rawResult.length) {
            return rawResult;
        }
        else if (okmLength < rawResult.length) {
            return rawResult.slice(0, okmLength);
        }
        else {
            return null;
        }
    }
    static expandFirst(prk, info, hmacAlgorithm) {
        let result = Buffer.alloc(info.length + 1);
        info.copy(result);
        result.writeUInt8(0x01, result.length - 1);
        return HKDF.hmacSHA256(result, prk, hmacAlgorithm);
    }
    static expandOnce(prk, info, preTemp, i, hmacAlgorithm) {
        let result = Buffer.concat([preTemp, info], preTemp.length + info.length);
        result.writeUInt8(i, result.length);
        return HKDF.hmacSHA256(result, prk, hmacAlgorithm);
    }
    static getHashLen(hmacAlgorithm) {
        switch (hmacAlgorithm) {
            case HKDF.HMAC_SHA1:
                return 20;
            case HKDF.HMAC_SHA256:
            default:
                return 32;
        }
    }
    static hexStringToBinary(hexString) {
        // 将16进制字符串转换为Buffer对象
        const buffer = Buffer.from(hexString, 'hex');
        // 返回Buffer对象
        return buffer;
    }
    static stringToBinary(hexString) {
        // 将16进制字符串转换为Buffer对象
        const buffer = Buffer.from(hexString, 'utf-8');
        // 返回Buffer对象
        return buffer;
    }
}
exports.HKDF = HKDF;
HKDF.HMAC_SHA1 = 'sha1';
HKDF.HMAC_SHA256 = 'sha256';
HKDF.DERIVATION_KEY_LENGTH = 32;
HKDF.HMAC_ALGORITHM = HKDF.HMAC_SHA256;
HKDF.ALGORITHM_HASH_LENGTH = HKDF.getHashLen(HKDF.HMAC_ALGORITHM);
HKDF.EXPAND_CEIL = Math.ceil(HKDF.DERIVATION_KEY_LENGTH / HKDF.ALGORITHM_HASH_LENGTH);
HKDF.hmacSHA256 = (str, key, hmacAlgorithm) => {
    return HKDF.hexStringToBinary(crypto
        .createHmac(hmacAlgorithm, key)
        .update(str)
        .digest('hex'));
};
//# sourceMappingURL=HKDF.js.map